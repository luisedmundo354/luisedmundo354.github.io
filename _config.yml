title: "Luis Brena"
description: "Research + projects in ML/NLP"
theme: jekyll-theme-dinky

# (Optional: add additional configuration variables as needed)

# GitHub Pages (user site) settings
# - Leave `baseurl` empty for `username.github.io` repos
# - Update `url` to your custom domain once configured
url: "https://www.luisbrena.com"
baseurl: ""

exclude:
  - CUSTOM_DOMAIN.md
  - Gemfile
  - Gemfile.lock
  - .bundle
  - vendor
